# 用户集中反馈修复计划（2026-09-15）

状态：本地候选已构建并通过软件短测；硬件验收及受限功能仍见第 8 节。分支 `codex/user-issues-repair-20260915`，基线 `6206b5a`。
范围：视频播放器、采集卡、PS5 串流、增强管线、视频导出和颜色管理。  
本计划不授权发布新版本、不替换运行组件、不修改显卡驱动。

## 1. 目标与原则

本轮目标是把用户反馈拆成可复现、可定位、可回归的工程任务。每项修复必须同时具备：

- 现象对应的日志或测试证据；
- 明确的状态、队列、时间戳和颜色契约；
- 可独立回归的自动测试或手工验收步骤；
- 失败时保留真实错误码，不用“回退成功”掩盖后端失败；
- 不把一台显卡、一个采集卡或一次成功启动当作全设备兼容证明。

播放器、采集卡和串流继续共享 `FrameSource -> EnhanceGraph -> FrameSink`。本轮不复制第二套增强链，也不通过无限队列或阻塞 UI 来掩盖性能问题。

## 2. 当前已知证据

### 已有证据

- 采集音频自动同步和启动积压已有独立修复候选；其测试使用合成采集时序和真实 WASAPI 静音输出，仍需反馈用户的真实采集卡复测。
- 现有串流异常日志中，视频已收到、D3D12VA 已解码，NVOF、SR、FG 初始化均返回成功；随后出现 `swapchain mode rejected requested=3 actual=4`，会话被取消。当前更像显示交换链模式回退处理错误，不能直接归咎于配对或网络。
- OBS 捕获兼容模式和窗口 resize 已有历史修复文档，但 1.2.0 后仍需用 1.1.0 对照确认是否回归。
- HDR/SDR 和 HDR10/5.1 已有颜色合同和施工记录，但用户反馈“都发灰”，必须增加端到端灰阶和色块证据。

### 尚未证明

- Dolby Vision 原生解码、RPU 保留和原生输出；不能用 HDR10 代称 Dolby Vision。
- RTX 4060/4070/4070 Ti 的真实过载边界；不能用 RTX 5070 或 RTX 50 代替 40 系验收。
- 用户导出日志中 `completed sample queue overflow` 与 BT.2020 错误的完整上下文；需要原始导出日志和输入媒体信息。
- “PS5 VRR 导致音频数秒延迟”的因果关系；需同一采集卡 VRR 开/关对照。

## 3. 分阶段施工顺序

### 阶段 A：不可用状态与恢复

优先修复会让用户无法继续使用的错误状态。

#### A1. PS5 串流增强初始化失败

现象：配对成功、收到视频，但点击增强后显示初始化失败或会话被关闭。

实施：

1. 将交换链能力探测、请求模式和实际生效模式分开记录。
2. SDR、HDR、直播兼容和窗口 resize 使用明确的回退顺序；实际模式改变时重新绑定呈现资源，不直接终止串流。
3. 利用已有传输/解码状态及增强/呈现日志分别定位失败阶段；本轮没有另建五层连接状态机。
4. NGX/NVOF/XeSS 失败只禁用对应后端，基础视频仍可显示。
5. 为每个阶段记录 HRESULT、NGX/XeSS 返回值、交换链格式、色彩空间和窗口尺寸。

验收：配对成功后无增强、NR、SR、DLSS FG、XeSS FG、HDR/SDR、全屏/窗口各启动一次；任何单项失败都不能关闭基础视频。

#### A2. OBS resize 回归

现象：1.2.0 起使用 OBS 游戏采集，放大缩小窗口后进入错误状态；1.1.0 无此问题。

实施：核查 resize 的实际资源所有权，等待相关旧资源 fence 后重建呈现缓冲；只有增强输入/工作尺寸改变才重建相应图和 reset 历史，纯窗口缩放不能一律重建全部增强。禁止旧 backbuffer 在 GPU 使用期间被释放或复用。

验收：连续拖动窗口、最大化/还原、全屏切换、DPI 改变、OBS 同时捕获；检查无黑帧、无错误状态、无旧尺寸资源访问。

#### A3. 采集卡断开自动重连

实施：检测 DirectShow 设备失效事件、视频样本停止和 WASAPI 端点错误；保留 UI/设置，按 1 至 5 秒封顶退避持续重试原设备，停止可中断等待。成功后恢复原视频格式并重置历史。设备路径或格式合同改变需要重新选择。独立 WASAPI 音频自行恢复；DirectShow 音频启动失败时保留视频，本轮未实现独立音频 pin 热重建。

验收：拔线/插回、设备被 OBS 占用、设备格式改变、音频端点单独断开、连续断开重连；不切到其他设备，不丢失用户选择。

#### A4. 采集卡选择记忆

实施：以稳定 `DevicePath` 为主键，显示名仅作展示；保存视频格式、帧率、像素格式和音频端点。启动时优先匹配上次设备，设备不存在时显示未连接，不静默选别的设备。

验收：重启软件、拔插设备、同名设备并存、设备顺序改变、旧配置升级；配置文件不得保存临时枚举序号作为唯一标识。

### 阶段 B：颜色与输入格式

#### B1. HDR/SDR 发灰

实施：每帧或每源显式记录 `matrix/range/transfer/format`，统一进入 linear working color；SDR 不走 HDR tone mapping，HDR 根据显示器能力选择 HDR 输出或明确 tone map 到 SDR；禁止重复转换。

验收素材：SDR 黑白灰阶、BT.709 色块、BT.2020/PQ、HLG、full/limited range。验收日志必须能解释输入、工作空间和输出，不以截图肉眼作为唯一证据。

#### B2. 视频导出 BT.2020 转换失败

实施：导出前以首个实际解码帧确定颜色合同，保留首帧供导出。SDR 支持 BT.601/709 和 BT.2020 NCL 转换；HDR 使用 PQ/HLG 到 HEVC Main10 HDR10。当前未新增 HDR 转 SDR 导出选择器；预览强制 SDR 不改变导出合同。BT.2020 CL 仍明确拒绝。

验收：导出输出帧数、PTS、颜色元数据和解码回验一致；HDR 输入不得触发 `BT.2020 input is not supported by BT.601/709 SDR conversion`。

#### B3. Dolby Vision

分三层交付：

1. 识别 Profile、基础层和 RPU 是否存在；
2. 先提供基础层兼容播放，或明确转换为 HDR10/SDR；
3. 只有确认 RPU 解码、编码器、Windows 显示链和授权后，才评估原生 Dolby Vision 输出。

完成前 UI 必须标明“兼容播放/转换”，不得宣称原生 Dolby Vision。

### 阶段 C：播放器交互和流畅性

#### C1. Seek、全屏进度和快捷键

实施：沿用拖动期间显示目标位置、松开提交与最新请求覆盖旧请求的机制；清理首帧缓存和过期 seek 结果。解码和 seek 在现有 EngineController 工作线程执行，UI 不等待；本轮没有增加独立预解码线程。全屏保留进度条、鼠标唤醒和键盘焦点；左右方向键默认前后10秒。

验收：4K 文件连续拖动、拖到末尾、反复前后跳、全屏/窗口切换、音频恢复；不出现先弹回旧位置再跳转的视觉回弹。

#### C2. 播放器流畅性

先测量再优化：记录解码等待、增强等待、GPU fence、呈现 deadline、UI 消息延迟、队列长度和丢弃原因。消除 UI 等待解码、每帧 CPU fence wait、旧 seek 结果提交和无界任务队列。

实时预览可以丢弃过期增强工作，但不能把导出路径改成丢帧，也不能放慢音频制造同步假象。

#### C3. UI 闪烁

排查 Win32 无效区域、交换链重建清屏、DPI resize 和控件重建。改为差异更新控件，画面层与 UI 层分离，交换链重建期间保留上一帧或明确的加载状态。

验收：模式切换、全屏、resize、错误提示、滚动和参数实时修改截图回归；检查无黑屏闪烁和控件跳动。

### 阶段 D：内部处理分辨率和性能

#### D1. NR 内部处理分辨率

增加 `480p / 720p / 900p / 1080p / 1440p` 挡位。它们表示 NR/增强工作尺寸，不改变最终输出尺寸；按源宽高比计算实际 extent，显示源/内部/输出三组分辨率。

每档记录显存、GPU 时间、实际处理 FPS 和丢帧；默认仍保持现有设置兼容，不擅自替换用户已保存的设置。

#### D2. 40 系过载

先区分真实 GPU 超时、队列积压、显存不足和错误阈值。记录每阶段 GPU 时间、显存预算/占用、队列深度、后端和驱动版本。持续过载沿用有界实时预览调度，并提示用户选择较低内部尺寸；不擅自修改已选分辨率或效果，不能直接把目标帧率锁30并伪装成正常。

验收至少覆盖 RTX 4060、4070、4070 Ti；本机没有对应实卡时只做代码和日志验证，不宣称硬件通过。

#### D3. FG/XeSS 计时与冗余

统一 GPU timestamp/fence 接口，分开显示 GPU 执行、CPU 提交、等待和呈现时间。后端不提供硬件时间戳时显示“未提供”，不能把0或未知参与总延迟。审计 NR、SR、光流、FG 是否重复等待或重复计入总延迟。

#### D4. FG 后端失败

按 NR/SR/FG/光流/NGX 核心/基础资源标注失败归属，原始 SDK/HRESULT/SEH 在日志保留。UI 显示后端及失败恢复提示，错误码当前需查看日志。健康 GPU 上禁用失败后端及其依赖，保留基础播放；设备丢失等基础故障仍报错。XeSS 呈现失败需丢弃未提交命令、重建调度器和交换链，不能仅 cancel 后继续使用已失败调度器。

### 阶段 E：视频导出队列

#### E1. completed sample queue overflow

源码确认该告警来自 `include/veyra/diagnostics/GpuTimer.h` 的 GPU 计时样本队列（上限64），并非视频帧队列。先核对订阅者和消费路径，修复无消费者时的订阅及计时样本丢失，日志明确区分遥测与视频帧。另独立审计真实导出帧队列与背压，不能凭此告警断言导出帧堆积。`flow=0 fallback=single-image` 需按实际来源与是否有历史判断，不能与样本溢出合并归因。

验收：导出长视频、4K、FG 开关、场景切换和 HDR；检查队列峰值、最终归零、帧数、PTS 连续性、取消清理和输出可解码。

## 4. 依赖关系与交付批次

### 批次 1：A1、A2、A3

目标：不再因串流初始化、窗口变化或采集卡断开进入不可恢复状态。

### 批次 2：B1、B2、C1

目标：先统一颜色，再修 seek/全屏交互；否则后续性能对比没有可信画面和时间线。

### 批次 3：A4、C2、C3、D1

目标：改善重复操作、低配置可用性和日常流畅度。

### 批次 4：D2、D3、D4、E1

目标：处理后端失败、40系过载、计时不完整和导出队列问题。

### 批次 5：B3

目标：在兼容播放和 HDR 转换稳定后再评估 Dolby Vision，不把未验证的原生输出塞进主线。

## 5. 每批次固定验收合同

- 构建成功，记录实际构建命令和依赖路径；
- 相关单元/集成测试通过，失败日志保留；
- 至少一个真实 GUI 短测；
- 记录输入、工作、输出分辨率与颜色合同；
- GPU/NGX/XeSS/NVOF 返回值和 HRESULT 进入日志；
- 不将运行组件、SDK、模型、用户配置或测试媒体提交源码 Git；
- 未接用户硬件时明确写“未执行”，不拿合成测试冒充实卡验收；
- 本轮不自动发布版本，发布需要另行明确授权。

## 6. 当前需要的外部证据

施工前优先收集：

1. 桌面“串流异常.log”已读取：呈现模式 requested=3 actual=4 被误拒绝，修复后还需反馈设备复测；
2. 视频导出失败的完整日志、输入文件信息和导出设置；
3. 4070/4070 Ti 过载日志，包含驱动版本、源分辨率、内部处理分辨率和所有效果开关；
4. HDR/SDR 发灰各一份源媒体或采集卡颜色日志；
5. OBS resize 回归的1.1.0与1.2.0日志；
6. 采集卡断连、重新插入和设备枚举日志。

在这些证据到齐前，可以先施工 A4、C1、C3 和日志/状态框架；不能声称已经解决用户设备上的 B1、D2、E1 或 A1。

上述文字是最初证据收集顺序；截至本次候选，本机可执行的修复和检查已推进，以下记录为实际交付状态。

## 7. 明确不做的事情

- 不通过增加缓冲解决卡顿；这会把卡顿变成更高延迟。
- 不用“系统 HDR 没开”解释所有发灰现象；必须先核对颜色合同。
- 不把 HDR10、HLG 或基础层播放宣称为 Dolby Vision。
- 不把一台 RTX 50 的结果推广到 RTX 40 全系列。
- 不用0、未知或软件提交时间伪造 XeSS/FG GPU 耗时。
- 不把导出队列溢出、flow fallback 和颜色转换错误合并成一个笼统的“性能不足”。

## 8. 逐项交付状态

**颜色结论修订：**用户随后报告同视频、无增强时Veyra偏暗。第6项及第9节的BT.1886旧测试只能证明代码按错误预期执行，不能证明播放颜色正确。已用同一实际视频的软/硬解逐像素和呈现缓冲检查推翻并修复；最新证据与候选身份以 `SDR_CODE_VALUE_ROUNDTRIP_REPAIR_2026-09-15.md` 为准，旧候选hash及色彩通过记录保留作追溯。

| 用户序号 | 本轮实现 | 当前证据与剩余验收 |
| --- | --- | --- |
| 1 采集卡记忆 | 原子保存稳定视频路径、格式语义键、音频端点及颜色选择；缺失时提示重新选择 | Unicode/配置往返测试通过；同名多卡、枚举变序待实卡 |
| 2 播放器 seek/流畅性 | 清除过期首帧缓存、跳过已被替代目标、记录解码至首帧耗时；保留 UI 即时目标 | 用户 4K 文件 6 次远距跳转及暂停连续跳转通过，157 至339ms；没有修改前对照，不宣称达到 PotPlayer 速度 |
| 3 UI 闪烁 | 正在播放的视频 surface 不再被 GDI WM_PAINT 涂黑；窗口 resize 不重建整条增强图 | UI 合同与 GUI 短测通过；尚未逐页视觉验收，不能认定所有闪烁消除 |
| 4 采集断开重连 | DirectShow 事件/3秒断帧触发同设备重连，1至5秒退避，恢复原格式/颜色合同及新 epoch；WASAPI 原端点持续恢复 | WASAPI 无效端点超过旧3次限制及中止退避测试通过；物理拔插未执行；DirectShow 音频单独初始失败没有自动 pin 重建 |
| 5 全屏进度/快进 | 沿用全屏自动隐藏/唤醒进度条；增加左右方向键前后10秒，不占用其他数值滑条或输入框 | 暂停 seek/resume 与 UI 合同通过；方向键实操待用户验收 |
| 6 HDR/SDR 灰 | 修正BT.1886工作解码与sRGB输出不互逆导致的压暗；文件与显式BT.709采集保留SDR代码值；保留HDR及广色域合同 | 真实文件软/硬解同帧最大偏差从13.31降至0.63级，呈现新增偏差0；HDR回归通过。其他播放器设置及用户设备仍需复测 |
| 7 NR 分辨率 | 480/720/900/1080/1440/native，保持宽高比，缩小内部 NR/flow，输出及导出尺寸不随档位缩水 | 设置往返、纵横比和导出尺寸合同通过；各档硬件性能曲线未测 |
| 8 OBS resize | 接受合法 flip 模式替代；暂时 ResizeBuffers 失败时保留旧缓冲并延迟重试，检查设备状态 | 同一窗口1920/2560/3840反复 resize、普通/捕获兼容模式真实GPU读回一致；未加载 OBS hook 复现反馈设备 |
| 9 Dolby Vision | 识别配置/RPU，P7及明确兼容的P8/P10走HDR10/HLG/SDR基础层；P5/未知兼容或颜色不匹配拒绝并说明 | 路由合同通过；真实DV媒体未验收。未实现 RPU/增强层重建或原生 DV 输出 |
| 10 FG 后端失败 | 初始化/运行按失败组件降低效果，UI显示实际应用设置；XeSS呈现失败重建后继续播放，可手动重新启用 | 真实 NR/XeSS 启用、注入故障、恢复、同会话重开均通过；注入故障不是反馈者真实硬件错误复现 |
| 11 XeSS计时/冗余 | 外层循环持续收取GPU计时，暂停/图像也消费；XeSS内部计时明确不可测，不填假0 | 合同与软件短测通过；SDK未提供内部GPU计时，不能声称实现精确XeSS耗时 |
| 12 导出问题 | 确认旧overflow为遥测队列；修正订阅消费和日志；导出由实际首帧确定HDR/bitdepth，完整保留源帧 | H.264/HEVC原生4K 2X帧数、音轨、取消检查通过；PQ导出30帧Main10/2020/PQ+5.1通过；反馈者原文件/完整日志仍缺 |
| 13 40系过载 | 用同帧GPU执行区间估算恢复预算，排除CPU迟轮询，把同帧FG成本从同帧总成本扣除，避免误锁补帧 | 本机NR+2X注入70ms阻塞后自动恢复，约13→46fps、生成约19fps；不是稳定60fps或40系实卡证明 |
| 14 PS5初始化失败 | 修复 requested=3 actual=4 合法flip交换链被误拒绝，后端失败可保留基础播放 | 本地交换链/恢复测试通过；未重新连接用户PS5实测，不宣称网络或配对故障根治 |

## 9. 实际验证与候选入口

最终构建目录：`out/build/audio-continuity-repair-20260915`。依赖仍为带PS5 slice补丁及dav1d的FFmpeg，RemotePlay ON，原有运行组件未替换。

命令均在项目根目录执行，单项限制不超过300秒：

```powershell
cmd.exe /c out\build\veyra-build-x64-release.cmd
out/build/audio-continuity-repair-20260915/veyra_repair_contract_tests.exe
out/build/audio-continuity-repair-20260915/veyra_ui_contract_tests.exe out/user-issues-ui-unique
out/build/audio-continuity-repair-20260915/veyra_wasapi_input_tests.exe --invalid
out/build/audio-continuity-repair-20260915/veyra_source_fidelity_tests.exe
# 下列集成测试经 scripts/run-short-test.ps1 包装，最长180秒：
# veyra_live_presentation_tests.exe <用户4K文件> <独立日志目录> --backend-recovery
# 同上 --seek-stress / --file-fg-recovery
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/gates/delivery.ps1 -Root . -BuildDirectory out/build/audio-continuity-repair-20260915
```

- 最终构建 `logs/user-issues-build-20260915-k.log` exit0。
- 合同137项失败0：`logs/user-issues-contract-i.log`；UI：`logs/user-issues-ui-j.log`；WASAPI：`logs/user-issues-wasapi-invalid-i.log`；重复resize：`logs/user-issues-resize-i.log`，均exit0。
- NR及XeSS完整恢复：`logs/user-issues-backend-recovery-k.stdout.log` 与同名目录 `engine.log`，12项全部通过，exit0。实际NGX Init/CreateFeature18返回0x1（Success），NVOF Create状态0；故障注入均明确标注 `backend-test`。
- seek：`logs/user-issues-seek-j/engine.log`，344.930/643.709/12/450/28/700秒目标首次呈现耗时266.533/157.017/284.033/339.187/236.143/288.996ms，解码96/48/112/150/92/106帧；暂停最新61秒目标221.771ms。关键帧至目标解码仍是主要等待来源。
- 过载恢复：`logs/user-issues-fg-recovery-h.log`；无重开音频、无修改所选配置即恢复补帧。
- HDR GPU：`logs/user-issues-hdr-color-20260915.log`；SDR/采集色块和源保真见对应 `user-issues-source-fidelity-20260915`、`user-issues-capture-color-20260915` 日志。
- 最终delivery：`logs/delivery/23d9cf72ba7a463aacda42f56be591e3/result.json`，47.98秒，软件短测通过，明确标注采集实卡待验收。先前j版也通过，本次因补XeSS恢复重跑。
- HDR导出：`scripts/run-short-test.ps1` 使用 `pq-tagged-51.mp4 --no-nr --no-sr --no-fg --hevc --export-out logs/user-issues-hdr-export-k.mp4`，60秒上限，exit0；外部ffprobe完整解码计数得到HEVC Main10/yuv420p10le/BT.2020 NCL/PQ/30帧、AAC 6声道5.1。
- 中途失败：ResetReason枚举名与缺少标准头导致构建失败已修复；backend-recovery-i因测试参数白名单遗漏提前exit2，补白名单后j/k通过。一次ffprobe使用了不存在的依赖bin路径，改用已安装CLI后成功；这些失败不计为通过。

本地入口：`out/start-user-issues-candidate.cmd`。根目录 `Veyra.cmd` 仍指向原x64-release，不能据此认定桌面旧入口已更新。最终候选EXE SHA256：`9DC754D09BB3835E605D32A8825040DFD25A24669D81789B8924D47351EEBAD3`。未push、未发布、未升版本、未关机。

## 10. 实机验收顺序与回退

1. 从候选入口启动，先不开增强，核对采集卡/格式/音频记忆、HDR/SDR颜色，再拔插同一设备。选WASAPI时另测音频端点恢复。
2. 4070/4070Ti从900p NR+2X开始，记录实际处理率/生成率、错误码和30秒以上过载恢复；随后对比1080p，不能以显卡型号预测必然达标。
3. OBS游戏捕获候选，连续最大化/还原、全屏/窗口；核对播放未进入错误状态。
4. 文件全屏方向键、拖动、暂停跳转；用原问题媒体导出，保留对应日志和完整设置。
5. PS5按已保存主机连接；失败时分别核对接收/解码/呈现，不要求重新配对掩盖显示错误。

代码回退基线 `6206b5a`；本轮在独立分支提交，验证期间保留原应用目录。不要重置用户未提交改动，不把回退扩大成删除运行组件/配对/配置。

下一步是以上问题设备的候选验收及按原始日志追踪剩余缺陷。原生Dolby Vision、XeSS内部计时、DirectShow音频pin独立热重建和PotPlayer级seek预解码仍未交付，不能将本轮报告称为14项全部实机修复完成。

## 11. 用户验收与审计更新

2026-09-15后续施工见 [独立补帧与恢复修复](FG_STANDALONE_AND_RECOVERY_REPAIR_2026-09-15.md)：FG选择遗漏开启总增强的UI根因已修，DLSS/XeSS均在NR执行0次时通过；音频分支恢复、GPU预算缺失样本、非NVIDIA实际设置及欠速文案补齐。完整软件gate通过，硬件和未实现功能边界继续保留。

用户2026-09-15确认第1、2、3项成功；全屏交互再次修正后确认第5项测试正常。最新逐项判定、已推翻的修复方向、未交付项及当前候选身份见 [14项复核](USER_ISSUES_REPAIR_AUDIT_2026-09-15.md)。以上旧状态/候选hash保留作追溯，不覆盖最新结论。第9项原生DV、第11项XeSS内部精确计时未完成；第4、6的HDR、8、10、12、13、14不具备全部问题设备验收证明。
